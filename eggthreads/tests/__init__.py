"""Test package for the :mod:`eggthreads` library.

For now the bulk of the low‑level tests for ``eggthreads`` live in the
top‑level ``egg/tests`` directory so they can share fixtures and avoid
importing unfinished modules.  This namespace file exists to honour the
requirement that each library has a ``tests`` package and to provide a
future home for more granular unit tests once the APIs stabilise.
"""
